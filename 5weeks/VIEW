--VIEW
--SCOTT2 계정에 VIEW를 만드는 권한 주기
GRANT CREATE VIEW TO SCOTT2;
--뷰 만들기(13-20)
CREATE VIEW VM_EMP20
AS (SELECT EMPNO, ENAME, JOB, DEPTNO FROM EMP WHERE DEPTNO = 20);

--뷰 확인
SELECT * FROM USER_VIEWS;

--삭제
DROP VIEW VM_EMP20;
